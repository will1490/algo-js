let min = prompt("What's your min ?");
let max = prompt("What's your max ?");
let current = prompt("What's your current ?");


if ((current <= max) && (current >= min)) {
    alert("current is between min and max !");
} if (min > max) {
    alert("Error : You don't understand !");

    close();
    
}